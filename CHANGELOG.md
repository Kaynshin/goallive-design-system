# goallive-design-system

## 0.3.0

### Minor Changes

- f1c1094: feat: distribute GoalGauge + RollingNumber (CSS in dist/goallive.css + vanilla builders `createGoalGauge`/`createRollingNumber` with types, exported via `./goal-gauge` and `./rolling-number`)

## 0.2.0

### Minor Changes

- 67619dd: Fondation du design system Goallive : tokens tricolores (encre/papier/signal), typographie Unbounded/Space Grotesk/Bricolage Grotesque, halo, motion, thème sombre par défaut + clair, primitives `.gl-*`, Storybook (Atomic Design), composants signature (RollingNumber, GoalGauge), tests Vitest + invariants de marque.
