---
"goallive-design-system": minor
---

feat: distribute GoalGauge + RollingNumber (CSS in dist/goallive.css + vanilla builders `createGoalGauge`/`createRollingNumber` with types, exported via `./goal-gauge` and `./rolling-number`)
